#include <stdio.h>
int main (){
    int x=50,y=80;
    if (x == 50)
    {
       if (y == 100 )
       {
           printf("x sayisi 50 y sayisi ise 100\n");

       }
       
    }
    printf("x sayisi %d,y sayisi ise %d",x,y);
    return 0;
}